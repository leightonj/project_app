<div class="card mb-3">
  <% if defined?(name) %><a name="<%= name %>"></a><% end %>
  <% if defined?(header) %><div class="card-header"><%= header %></div><% end %>
  <div class="card-body">
    <%= yield %>
  </div>
</div>
